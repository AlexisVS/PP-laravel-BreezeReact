import React from 'react';
import React from 'react';
import { render } from 'react-dom';

const Application = () => {
  return (
    <div>
      <h1>Bonjour J'ai réussi a installer reactJS sur une application PHP Laravel</h1>
    </div>
  );
};

export default Application;

const mainApp = document.getElementById('mainApp');